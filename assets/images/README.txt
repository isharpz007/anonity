Put your logo file here as:

  logo.png

(any resolution works, but 512x512 or larger recommended so it stays
sharp on the splash screen, which renders it at ~130px but on large
tablets/desktop could scale up)

The app looks for exactly this path: assets/images/logo.png
That's registered in pubspec.yaml under flutter: -> assets:
